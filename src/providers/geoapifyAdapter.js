(function(root,factory){const api=factory(root);if(typeof module==='object'&&module.exports)module.exports=api;if(root){root.JiaProviderAdapters=root.JiaProviderAdapters||{};root.JiaProviderAdapters.geoapify=api;}})(typeof window!=='undefined'?window:null,function(root){async function search(place,missingFields){if(!root?.JIA_ENRICHMENT_PROXY_URL)return {status:'disabled_no_proxy'};const response=await fetch(root.JIA_ENRICHMENT_PROXY_URL,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action:'enrich_place',provider:'geoapify',place:{name:place.name,location:place.location},missingFields})});if(!response.ok)throw new Error(`Geoapify proxy ${response.status}`);return response.json();}return {search};});
